from django.apps import AppConfig


class SupervisionappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.supervisionApp'
    verbose_name= "نظارت و بازرسی"

